version: 0.2
phases:
  pre_build:
    commands:
      -echo "Pre-build Phase"
    build:
      commands:
        -echo "Build Phase"
        -mvn clean prepare-package war:explode
      post_build:
        commands:
          -echo "Post Build"
          -mv target/deploy ./
    artifacts:
      files:
        -deploy/**/*
